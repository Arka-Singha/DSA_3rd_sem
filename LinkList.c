//Link List containing p nodes
#include<stdio.h>
#include<stdlib.h>
struct node
{
    int data;
    struct node *next;
};
struct  node*head=NULL;
void main()
{
    insert_end(38),
}

insert_end(int val)
{
    struct node *temp;
    temp=malloc(size of (struct node));
    temp->data=val;
    temp->next=NULL;
    struct node *ptr=head;
        while(ptr->next !=NULL)
            {
                ptr=ptr->next;
                ptr->next=temp;
            }
    void display()
    {
        sturct node *temp=head;
        while(temp->next!=NULL)
        {
            printf("%d->",);
        }
    }
}