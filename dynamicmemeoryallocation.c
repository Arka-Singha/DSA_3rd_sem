// malloc
// float *ptr=

#include<stdio.h>
void main()
{
    int *ptr;
    ptr=malloc(size of (int)*4);
    for(int i=0; i<4; i++)
        {
            printf("The address of %d element is: %d",i+1,ptr+i);
        }
}
